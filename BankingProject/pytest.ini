# pytest.ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = -v --tb=short

markers =
    smoke: smoke tests
    regression: regression tests

filterwarnings =
    ignore::DeprecationWarning